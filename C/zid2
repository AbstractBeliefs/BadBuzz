#include <stdio.h>

int main(void)
{
	int i;

	for(i = 1; i < 100; i++)
	{
		int a, b;

		a = i%3;
		b = i%5;

		printf(a&&b ? "%d\n" : "%s\n", a&&b ? i : (a ? "buzz" : b ? "fizz" : "fizzbuzz"));
	}

	return 0;
}
